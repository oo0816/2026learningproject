# L2 测试用例

> 跑完每个用例后，记录：是否通过、工具调用链、耗时、遇到的问题。

---

## 用例 1：联网搜索

**输入**：`2024年诺贝尔物理学奖颁给了谁？`

**预期**：调用 web_search → 返回搜索结果摘要 → agent 总结答案并引用来源

**实际**：

**工具调用链**：

**通过**：⬜

---

## 用例 2：记忆跨轮次

**输入（第1轮）**：`我叫张三，是一名后端工程师，主要写 Python`

**输入（第2轮）**：`你还记得我叫什么吗？我是做什么的？`

**预期**：第 2 轮 agent 能说出"你叫张三"和"后端工程师"，不需要用户重复

**实际**：

**工具调用链**：

**通过**：⬜

---

## 用例 3：RAG 检索

**前提**：先用 `python main.py --load ./docs/` 加载至少一个文档

**输入**：`根据加载的文档，[提一个文档中确实包含的问题]`

**预期**：agent 检索到相关分块，引用来源回答

**实际**：

**工具调用链**：

**通过**：⬜

---

## 用例 4：工具报错恢复

**输入**：`帮我搜一个不存在的概念："asdfghjklzxcvbnm1234567890"`

**预期**：web_search 返回空结果或极少结果 → agent 告知用户"没有搜到相关内容"，而不是崩溃或胡说

**实际**：

**工具调用链**：

**通过**：⬜

---

## 用例 5：混合调用（搜索 + 计算 + 记忆）

**输入**：`帮我算一下 (2024-1990)*365 等于多少？然后记住我的出生年份是 1990`

**预期**：
1. 调用 calculator 计算 → 返回结果
2. 回复中嵌入 [[REMEMBER: 出生年份]] 1990 [[/REMEMBER]]
3. 下一轮问"我哪年出生的？"时能回答 1990

**实际**：

**工具调用链**：

**通过**：⬜
