"""
L6 · 最终 Agent 项目
====================

## 这是整个学习路线的终点

你要交付一个别人能 clone 下来、配好 key 就能跑的真实 agent 项目。

## 选题（三选一）

### 选项 A：膝关节导航术前报告 Agent
输入：影像分析结果（结构化数据或文本描述）
输出：自动生成的结构化术前报告
审核：医生确认/修改
关键挑战：对准确性要求极高，必须有引用来源

### 选项 B：简历智能匹配 Agent
输入：求职者简历 + 岗位 JD
输出：匹配度分析 + 改进建议 + 面试问题推荐
关键挑战：需要理解不同行业的术语差异

### 选项 C：个人知识助手 Agent
输入：用户问题
处理：RAG 检索用户笔记/文档 → 回答并引用来源
关键挑战：文档格式多样（md/txt/pdf）、检索精度

## 项目要求（缺一不可）

- [ ] main.py 可直接运行（pip install + 配 key + python main.py）
- [ ] README.md 写清楚：怎么跑、怎么配 key、怎么扩展工具、有哪些限制
- [ ] tests/ 目录下至少 5 个测试用例（不只是手动跑，要有自动化测试）
- [ ] eval_report.md：20 个测试用例的成功率、失败分类、成本统计
- [ ] SKILL.md：把项目打包成可复用的 skill
- [ ] 架构图（agent loop + 模块交互）
- [ ] 权限边界说明：什么操作要确认、什么自动执行、什么完全禁止

## 架构建议

你已经在 L1-L5 中实现了所有这些模块的原型，L6 是把它们组装成最终产品：

```
┌─────────────────────────────────────────────┐
│                L6 Final Agent                │
│                                              │
│  ┌────────────┐  ┌──────────┐  ┌──────────┐ │
│  │ Agent Loop │  │  Tools   │  │  Memory  │ │
│  │ (L3 改造)  │  │ (L2 升级) │  │ (L2 升级) │ │
│  └────────────┘  └──────────┘  └──────────┘ │
│         │              │             │       │
│  ┌────────────┐  ┌──────────┐  ┌──────────┐ │
│  │Permission  │  │   RAG    │  │  Logger  │ │
│  │(L3 实现)   │  │ (L2 升级) │  │ (L3 实现) │ │
│  └────────────┘  └──────────┘  └──────────┘ │
│                                              │
│  ┌──────────────────────────────────────┐   │
│  │        Eval Layer (L5 升级)           │   │
│  │   20 测试用例 / 成功率 / 成本统计      │   │
│  └──────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

## 你的任务

1. 选一个选题方向
2. 从 L1-L5 的代码中挑选可复用的模块，改造适配
3. 实现完整的 agent loop + 工具 + 记忆 + RAG + 权限
4. 写完善的文档和测试
5. 用 darwin-skill 评分，迭代到 ≥ 70 分
6. 找至少一个真实用户跑你的项目并给反馈
"""

import os
import sys
import json

sys.stdout.reconfigure(encoding="utf-8")

# ══════════════════════════════════════════════════════════════
# 项目骨架
# ══════════════════════════════════════════════════════════════
#
# 你的 L6 项目应该至少包含以下文件：
#
# l6-final-agent/
# ├── main.py          ← 入口，agent 核心逻辑
# ├── tools.py         ← 针对选题的专用工具
# ├── memory.py        ← 三层记忆（从 L2 改造）
# ├── rag.py           ← RAG 检索（从 L2 改造）
# ├── permissions.py   ← 权限控制（从 L3 改造）
# ├── config.py        ← 配置管理（API key、模型选择等）
# ├── README.md        ← 项目文档
# ├── SKILL.md         ← 把项目打包成可复用 skill
# ├── tests/
# │   ├── test_agent.py
# │   └── test_tools.py
# └── eval_report.md   ← 20 个测试用例的评测报告
#
# 不要试图第一次就写完美。先选最简单的选题，
# 跑通最小可用版本（1 个工具 + 基本对话），再逐步加功能。

# ── 最小可用版本 ——
def main():
    """项目入口。先跑通这个，再加功能。"""
    print("=" * 55)
    print("L6 · 最终 Agent 项目（选题：________）")
    print("=" * 55)
    print()
    print("第一步：选一个选题方向（A/B/C）")
    print("第二步：从 L1-L5 挑选可复用的模块代码")
    print("第三步：实现最小可用版本（1个工具+基本对话）")
    print("第四步：加记忆、RAG、权限、测试")
    print("第五步：写文档（README + SKILL.md + eval_report.md）")
    print("第六步：darwin-skill 评分 ≥ 70 + 真实用户反馈")
    print()
    print("加油！这是最后一关了 🎯")


if __name__ == "__main__":
    main()


# ══════════════════════════════════════════════════════════════
# 常见坑
# ══════════════════════════════════════════════════════════════
#
# 坑 1：选题太大，一周做不完
#   → 先做最小可用版本，再加功能。一个能跑的烂项目 > 一个完美的空项目。
#
# 坑 2：文档写得太简略
#   → README 要能让一个陌生人（比如面试官）clone 下来跑通。
#   → 写清楚：依赖安装、API key 配置、运行命令、常见问题。
#
# 坑 3：没有自动化测试
#   → 手动跑 20 个用例很痛苦且不可重复。
#   → 用 pytest 写自动化测试，CI 能跑的那种。
#
# 坑 4：权限边界不清晰
#   → 文档里必须写清楚：什么操作自动执行、什么要确认、什么禁止。
#   → 这是产品的安全底线，不是可选项。
#
# 坑 5：为了凑 eval 数量写了很多无关紧要的用例
#   → 20 个用例要有意义。包含：正常流程、边界情况、异常场景、多轮对话。
#   → 每个用例都要能解释"如果不测这个，线上可能出什么问题"。
